# =============================================================================
# Autonomous AI Issue Resolver - Production Grade
# Optimized for: Debian + RTX 3070 + Ollama 
# =============================================================================

name: Autonomous Aider Agent

on:
  issues:
    types: [opened]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  resolve-issue:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git for Autonomous Agent
        run: |
          git config --global user.name "Aider-Bot"
          git config --global user.email "aider-bot@local.cluster"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Execute Aider Orchestrator
        env:
          # Usamos las rutas absolutas que confirmamos con 'which' 
          PYTHON_BIN: /usr/bin/python3
          AIDER_BIN: /home/nromero/.local/bin/aider
          OLLAMA_API_BASE: "http://127.0.0.1:11434"
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          # 1. Crear rama dinámica
          BRANCH_NAME="feature/issue-$ISSUE_NUMBER-auto-resolve"
          git checkout -b "$BRANCH_NAME"
          echo "BRANCH_NAME=$BRANCH_NAME" >> $GITHUB_ENV
          
          # 2. Ejecutar usando la ruta absoluta de Python 3
          $PYTHON_BIN scripts/agent_orchestrator.py

      - name: Push Branch and Create Pull Request
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 3. Empujar cambios y crear PR (indentación corregida)
          git push origin ${{ env.BRANCH_NAME }}
          
          gh pr create \
            --title "Fix: ${{ github.event.issue.title }} (Auto-Resolved)" \
            --body "This PR was autonomously generated by the local Aider agent to resolve issue #${{ github.event.issue.number }}." \
            --head ${{ env.BRANCH_NAME }} \
            --base main
